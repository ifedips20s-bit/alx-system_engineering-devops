# 0x03 Shell Variables & Expansions

## 0-alias
Creates an alias `ls` that runs `rm *`. To activate it, source the script using `source ./0-alias`.

## 1-hello_you
Prints "hello" followed by the current Linux username using the $USER variable.